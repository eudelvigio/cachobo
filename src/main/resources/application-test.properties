spring.jpa.properties.jadira.usertype.autoRegisterUserTypes = true
uploads_directory=/var/www/test_wanabet/uploads/
uploads_base_url=https://test.wanabet.es/uploads/
bo_base_url=https://test.wanabet.es/wanaba/public/
spring.http.multipart.max-file-size=128MB
spring.http.multipart.max-request-size=128MB

spring.datasource.url= jdbc:mysql://89.17.213.35:3306/wpbodev
spring.datasource.username=wpbodevadm
spring.datasource.password=w1CnmlP-m
spring.datasource.driverClassName=com.mysql.jdbc.Driver
# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = false

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
#spring.jpa.properties.hibernate.physical_naming_strategy = com.rf.configuration.prefijators.TestPrefijatorNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

#Go by another routeroot
server.contextPath=/wanaba

server.port = 8085